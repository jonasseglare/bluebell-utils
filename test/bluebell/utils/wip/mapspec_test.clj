(ns bluebell.utils.wip.mapspec-test
  (:require [bluebell.utils.wip.mapspec :refer :all]
            [clojure.test :refer :all] :reload))

;; (specify Kattskit)

;; (deftest kattskit-test
;;   (is (= {} (Kattskit {})))
  
;;   )

;; (def k (field render-settings))

;; (specify PlotState
;;   k)
