(ns bluebell.utils.mapspec-test
  (:require [bluebell.utils.mapspec :refer :all]
            [clojure.test :refer :all] :reload))

;; (specify Kattskit)

;; (deftest kattskit-test
;;   (is (= {} (Kattskit {})))
  
;;   )

;; (def k (field render-settings))

;; (specify PlotState
;;   k)
